#include "main.h"

/**
 * mul - multiply two whole numbers
 * @a: first operand
 * @b: first operand
 *
 * Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	int mult = a * b;

	return (mult);
}
